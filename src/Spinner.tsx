import { Box } from '@chakra-ui/react';
import React from 'react';

type tSpinnerProps={
// pass
};

export const Spinner: React.FC<tSpinnerProps> = () =>{
    return (
        <Box>
                Заждіть, завантажуємо

        </Box>
    );
};